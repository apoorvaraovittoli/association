<h1>Editing Birthday</h1>

<%= render 'form', birthday: @birthday %>

<%= link_to 'Show',teacher_student_birthday_path(@teacher,@student,@birthday) %> |
<%= link_to 'Back', teacher_student_birthdays_path(@teacher,@student) %>
